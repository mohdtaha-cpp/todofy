/**
 * The component creates the header of the application.
 * 
 * @returns Header Component
 */
const Header = () => {
    return (
        <header>
            <h1 className="font-medium text-xl md:text-4xl">ToDofy Application</h1>
        </header>
    )
}

export default Header;
